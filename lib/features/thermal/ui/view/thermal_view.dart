import 'package:flutter/material.dart';

class ThermalView extends StatelessWidget {
  const ThermalView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Column(children: [Text('Thermal')]));
  }
}

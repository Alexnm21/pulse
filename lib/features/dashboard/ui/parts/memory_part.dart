part of '../view/dashboard_view.dart';

class MemoryPart extends StatelessWidget {
  const MemoryPart({super.key});

  @override
  Widget build(BuildContext context) {
    return CrystalCard(child: SizedBox());
  }
}
